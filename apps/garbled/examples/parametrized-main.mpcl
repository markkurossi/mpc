// -*- go -*-

package main

type Garbler struct {
	a []byte
	b []byte
}

func main(g Garbler, e []byte) (int, []byte) {
	l := len(g.b)
	if len(e) < l {
		l = len(e)
	}
	result := make([]byte, l)

	for i := 0; i < l; i++ {
		result[i] = g.b[i] ^ e[i]
	}
	return l, result
}
