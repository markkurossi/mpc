// -*- go -*-

package main

import (
	"encoding/binary"
)

func main(a, b uint) uint {
	return hamming(a, b)
}

func hamming(a, b uint_) uint {
	var sum uint = 0

	for i := 0; i < size(a); i = i + 1 {
		if a[i:i+1]^b[i:i+1] != 0 {
			sum++
		}
	}
	return sum
}
